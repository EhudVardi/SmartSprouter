#ifndef PAGES_ENUMERATION_H
#define PAGES_ENUMERATION_H

#include <unordered_map>
#include <string>

enum class Pages {
    About,
    Idle,
    Setup,
    Run,
    Abort,
    Error,
    Diag,
};

#endif // PAGES_ENUMERATION_H